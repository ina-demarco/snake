package tests;

import snake.GameObject;

public class GameObjectTest {
	private GameObject gameObject;

	/*
	 * Refactored to abstract
	 * 
	 * @Before public void setup(){ gameObject = new GameObject(1,1); }
	 * 
	 * @Test public void testCreateGameObject(){
	 * assertEquals(gameObject.getPositionX(), 1);
	 * assertEquals(gameObject.getPositionY(), 1); }
	 */

}
